# Harness instruction-prose audit — final report

Date: 2026-08-31 03:39–04:0x · Chain: unlazy gates → hooks-audit → skill-analyzer + skill-cleaner → config-prose (inline) → agents-styles (delegated) → contradiction matrix (inline) → opus-validate. Read-only against all audited surfaces; every artifact under `plans/`.

Leaf reports: `hooks-audit-…txt` · `skill-analyzer-…md` · `skill-cleaner-…txt` · `config-prose-…md` (17 findings) · `agents-styles-…md` (49 findings) · `contradiction-matrix-…md` (98 pairs). This file synthesizes; the leaves carry the citations.

## Executive summary

The harness's deployed instruction prose is directionally strong where it was recently engineered (SKILL-FIRST order, ADHD output style, RULES.md's military register) and weak in three systemic ways: **(1) unpriced duplication** — the same authority restated across 2–4 always-loaded surfaces, ~13K tokens of rules prose per session before any hook fires; **(2) enforcement asserted in prose where only the tool layer can enforce** (vn-* path fences, validator model floors, ocx skill deny-lists); **(3) drift between copies** — 4 rule files deployed twice with divergent content, a stale rules count, a stale keep-on note. True contradictions exist and every one has a one-line directional fix. Totals: **16 config findings (C6 reclassified informational post-validation) + 49 agents/styles findings + 26 matrix rows; 12 distinct HIGH items** (3 config + 9 agents/styles; the matrix's 8 HIGH rows restate these, adding no new defects).

## Hooks

- 59 hook entries across 14 events; 45 unique live event×script pairs (~30 distinct scripts) once the dormant families are excluded. Live per-turn injectors: `dev-rules-reminder.cjs`, `mnemosyne/recall-inject.py`, `secret-output-guardrail.cjs`, `simplify-gate.cjs`, `usage-quota-cache-refresh.cjs`, `caveman-vn-carveout.js` (UserPromptSubmit) plus SessionStart/Stop/PreToolUse guards.
- **2 duplicate registrations** (hooks-audit report): `secret-output-guardrail.cjs` ×2 and `simplify-gate.cjs` ×2 on UserPromptSubmit — double execution every turn.
- The `.orca/agent-hooks/claude-hook.sh` (12) and `SUPERSET_HOME_DIR/hooks/notify.sh` (8) families are **guarded conditionals** for external supervisors, not dead weight — hooks-audit's naive "missing files" read over-flags them; both wrap in existence checks (`[ -n "$SUPERSET_HOME_DIR" ] && [ -x … ]`).
- Hook-emitted prose findings: C2 ("Sacrifice grammar…" contradicting the orthography mandate), C11 (config values `mode=prompt, questions=3-8`, `docs.maxLoc: 800` injected with no attached instruction), C12 (cache-ttl "report this in your first reply" — no checkpoint, observed missed this session), plus per-turn double-injection of development-rules content (matrix pair 10).

## Skills

- Census: 371 skill dirs under `~/.claude/skills`; cleaner sees **705 discovered / 600 considered** across roots. Description budget: **99.9% of the 2% cap consumed; unbudgeted full descriptions = 66,604 tokens (1224% of budget); 442 skills omitted after budget** — retrieval-layer descriptions are the constraint, not the count.
- Description sprawl: `vn-deep-dive-report`'s description (1,114 chars) appears 5× in the cleaner's candidate list — four `.backup-*` directories plus the live SKILL.md. Backup-dir sprawl under `~/.claude/skills/` inflates the census (a share of why cleaner discovers 705) and wastes description budget; purging/ignoring `.backup-*` dirs is a direct recover. (Corrected post-validation: first attributed to vn-canu — the cleaner's path column names vn-deep-dive-report.)
- Cross-root name duplicates exist (codex/codex-plugin vs default) — cleaner resolves keep-default throughout.
- Tool-layer defect: `skill-analyzer` run at namespace-root scale produces a mangled single-skill report — it parsed prose words as commands (`/the`, `/that`, `/one` from "…the directory…") and graded the garbage B(85/100). Its output at this scale is not evidence; per-skill invocation still works.
- Keep-on: 41 always-on skills; `keep-on.json` `_note` still describes `ck:/vn:` entries that no longer exist (snapshot 2026-06-26, drift).

## Config prose

17 findings (C1–C17) in the leaf report; the three HIGH: C1 token-economy contradiction (workbench × RULES [49]), C2 grammar-vs-orthography (hook × system mandate), C3 four rule files deployed twice with divergent content (`rules/` auto-loaded vs `docs/` on-demand). Notable MEDs: undefined term "brainstorm contract" (skill-workflow-routing:14), harness-specific "cook skill" alias inside a global rule (primary-workflow:22), anti-patterns.md 300+-word table cells, RULES.md:85 rotation-chain off-by-one. (C6 "stale 77 count" was withdrawn at validation: RULES.md carries 77 distinct live rule indexes — the header is accurate under that unit.) Always-loaded corpus: CLAUDE.md (194 ln) + RULES.md (162 ln) + 10 rules files = **734 lines / 51,409 bytes ≈ 13K tokens every session** — unpriced, which contradicts RULES [48]'s own pricing mandate (matrix pair 23).

## Agents & output styles

49 findings over 41 files (9 HIGH / 22 MED / 18 LOW), full coverage, 81 `file:line` citations — leaf report. Top leverage:
1. All 41 files lack a Layer-1 outcome block (`Goal:/Success means:/Stop when:`) — "done" drifts turn-by-turn.
2. Coding-level ladder (6 styles, 771 lines): 153 decorative ALWAYS/NEVER/MUST hits bleach the invariant words.
3. vn-* family: 169 negations across 4 researchers + validator; prohibitions planted verbatim where positive contracts fit.
4. Three validators + five ocx clones duplicate gate/deny-list prose that belongs in metadata or a disclosed reference.
5. Five agents (explore/goal-scout/goal-judge/librarian-cataloger/researcher) compete for one scouting capability.

## Contradiction matrix

98 distinct pairs checked (78 mechanical + 26 deep). 8 HIGH relationships: token economy (pair 1), grammar mandate (2), the 4 divergent file pairs (3–6), god-style × concise/ADHD (13), vn-* fences × tool grants (17) — each restating a config or agents-styles HIGH, none new. Dominant pattern: unpriced duplication, not contradiction — which is exactly what hides the true contradictions at injection time. Verdict in the matrix file: no unrecoverable conflict; every HIGH has a directional fix.

## Fix roadmap (ranked, none applied — audit-only mandate)

1. ~~Delete one side of each `docs/` ↔ `rules/` divergent pair; make `rules/CLAUDE.md` index point at survivors (kills 4 HIGH drifts).~~ **DONE 2026-08-31** — stale docs/ copies archived to `~/.claude/_Archived/rules-docs-dupes-260831/`, index repointed, hook-verified; plan at `plans/260831-1735-rules-docs-dedup/plan.md`.
2. ~~Fix `hooks/dev-rules-reminder.cjs` prose~~ **DONE 2026-08-31, backport same day** — fixed in `hooks/lib/context-builder.cjs` (the builder behind the hook): "Sacrifice grammar" line deleted; KISS/DRY restatement → pointer to development-rules.md; `docs.maxLoc: 800` → "keep docs files under 800 lines"; `Validation: mode=prompt, questions=3-8` → "when reviewing a plan, ask 3-8 questions before locking it". Live hook verified end-to-end (exit 0, mock payload). **Backport:** 7 further live copies patched (`.codex` harness hooks, `.claude/.claude` nested, `-claude`, claudekit-engineer source, claudekit-marketing, Diep.Wealth, DESIGN-WORK-VGP `.opencode` plugin ×2 incl. its stray duplicate) — two passes (global-identical cp + line-match for the drifted `YAGNI-KISS-DRY` vintage), each with a `.bak-260831` beside it, all syntax-checked and render-verified with the devRulesPath guard. Untouched by design: extracted-kit archives, `referencing-plugins/`, LANDING_ZONE snapshots (reference/backup material). Scripts + diffs: `MY-WORKBENCH/.scratch/hook-fix-260831/` (`propagate.py`, `propagate2.py`).
3. ~~Reconcile workbench token-efficiency block with RULES [49] into one positive statement.~~ **DONE 2026-08-31** — the 7-bullet negation block (CLAUDE.md:52-60) replaced in BOTH workbench twins (CLAUDE.md + AGENTS.md, real drifted copies, each with `.bak-260831`) by one positive statement: "Economize redundancy only — never scope, verification, or reporting depth (RULES.md [49] governs; the Completion-discipline HARD RULES above outrank this block). Trust a check you just ran unless its outcome was uncertain or a gate requires re-verification; batch related edits into one pass." Dropped as [49]-conflicting: "If a task needs 1 tool call, don't use 3". Re-verification now explicitly allowed when a gate requires it (was in tension with [47]/unlazy reverify). NEW FINDING surfaced: workbench AGENTS.md is a stale twin (still documents the retired agentmemory system at lines 64-73; CLAUDE.md carries the memsearch sections) — out of item-3 scope, flagged for owner. **RESOLVED 2026-09-01:** AGENTS.md synced byte-identical to CLAUDE.md (backup `AGENTS.md.bak-260901`). Dropped from AGENTS.md with liveness evidence: agentmemory sections (plugin absent, port 3111 dead), cognee guard block (`UV_CONSTRAINT`/`UV_OVERRIDE` pins no longer in settings.json — dir `~/.cognee-plugin/` itself untouched), IJFW managed blocks (no ijfw install, no `.ijfw/` dir anywhere). Gained: memsearch sections, extended sub-agent-mandate clause, graphify-caveats block. Cosmetic note: line 1 H1 reads "# CLAUDE.md" in both files — kept identical deliberately; rename would re-diverge the twins. Residual drift risk: twins are real copies; a symlink (owner's taste call) would prevent recurrence.
4. ~~Enforce-at-tool-layer migrations~~ **DONE 2026-09-01, with two audit corrections** (docs-verified against code.claude.com/docs/en/sub-agents this turn: no per-agent path scoping or per-agent skill deny exists in frontmatter). (a) Model floors → metadata: `agent-validator.md` `model: inherit`→`opus` (description said "Opus-powered"; metadata now enforces it), `tk-validator.md` gained `model: opus` + guard comment (edited at its dev target `skills-dev/tk-research/agents/`). (b) Path fences: vn-bctt + vn-news researcher NEVER-write lines → positive contract form ("Every write resolves inside {plan_dir}… abort and return the error JSON"), edited at dev targets in `skills-dev/agents/`; class-swept — vn-canu/vn-validator carry no fence. (c) CORRECTION of H-1/H-2: the vn HARD FLOOR comments guard a live `model: opus` field (maintenance instruction, not prose-enforcement) — deliberate, no change. CORRECTION of H-9: the ocx deny line is GENERATED from opencodex's `blockedSkills` roster (`agents-inject.ts:167`) — already router-level, single-sourced; negative phrasing is upstream's template (npm package, local patch would be wiped) — upstream-issue candidate, no local edit. Backups: `~/.claude/_Archived/agents-item4-bak-260901/` (9 files).

**REVISED same day (user correction — cross-harness):** a hardcoded `model: opus` in agent frontmatter fails the agent launch entirely on non-Claude harnesses (user-observed). Item-4a's two metadata additions were REVERTED and the whole 12-file `model: opus` class migrated to spawn-time enforcement (user chose "migrate all 10" + the 2 reverts): every agent frontmatter is now model-neutral, carrying a MODEL FLOOR comment recording the order + revert path; the explicit-model mandate now lives at the dispatch sites — opus-validate (already had it), tk-research SKILL.md, and a uniform "Model floor (family-wide)" paragraph inserted at 10 spawn sites across 8 vn skills (bctt ×2, canu ×2, signals, deep-dive, ares, b3, baleba, concise). Side finds fixed en route: installed `vn-bctt-concise` was a stale v1.0 copy of dev v1.0.1 — replaced with a symlink per the install rule; vn-author/vn-editor dev-vs-installed pairs confirmed identical and both patched. Final sweep: zero `model: opus` in live agent surfaces. Backups: `~/.claude/_Archived/model-floor-migration-260901/` (13 items).
5. ~~Collapse one authority per lane~~ **CLOSED 2026-09-01 — both owner decisions in, both evidence-driven**: (a) goal-scout/goal-judge MERGE **withdrawn** at full-source read (distinct roles pinned by exact-name dispatch contract + codex toml twins; leaf's "80% shared" was contract-frame similarity) — owner confirmed NO CHANGE; (b) validator-gate EXTRACT remains the sole open pick. Map: `plans/reports/lane-consolidation-map-260901-0331.md`.
6. Add Layer-1 outcome blocks to the 41 agent/style files (shared template).
7. ~~Hygiene batch~~ **DONE 2026-09-01** (user-modified scope for the backup dirs: move-only, no delete): (a) dup hooks — the two "duplicate registrations" were actually a fully-contained redundant entry ({guardrail, simplify-gate} ⊂ the larger dev-rules entry); whole entry removed, every UserPromptSubmit script now ×1, JSON round-trip validated; (b) CLAUDE.md:4 garble fixed ("allways/at all time" → clean sentence); (c) RULES.md:85 "4th"→"5th" (edited at real target `in-PROD/MY-WORKBENCH/RULES.md` — `~/.claude/RULES.md` is a symlink); (d) keep-on `_note` refreshed (stale ck:/vn: + snapshot-date references dropped; apply-overrides.py confirmed not to parse `_note`); (e) 12 stale skill-backup dirs MOVED to `~/.claude/_Archived/skill-backup-dirs-260901/` with parent-prefixed collision-safe names (11 nested `.backup-*` + 1 `_shared-vn.stale-backup-2026-06-11`; the second same-named path was a mirror twin — one physical copy archived). The withdrawn "77 rules" count stayed excluded. Backups: `.bak-260901` beside settings.json, CLAUDE.md, RULES.md, keep-on.json. Effect of 1a lands next session (settings read at startup).

## Unresolved questions

- U1 (owner): Should Layer-1 outcome blocks live per agent file or behind one disclosed `references/agent-outcome-template.md`?
- U2 (owner): Confirm `casual-vietnamese.md` intended scope (global opt-in vs per-turn) — determines fix for H-7. (Validator note: the style's line 3 frontmatter does carry a code/CLI/commit carve-out; H-7's "no scope sentence" premise was weakened by quote truncation in the leaf — treat H-7 as documentation-polish, not policy risk.)
- U3 (owner): The 12 `.orca` + 8 `SUPERSET` guarded hook registrations — confirm they're still wanted; if the orca/supervisor integrations are retired, removing them simplifies every event's hook chain.
- U4 (practice): RULES [48] pricing mandate vs deployed stack — adopt a standing per-turn injection budget line, or amend [48].

## Validation record

Independent Opus validator (`opus-validate` chain step): initial VERDICT **FAIL — 2 BLOCKING, 7 ADVISORY**. Both BLOCKING items and six of seven advisories were corrected in this file and the config leaf (HIGH count 10→12, matrix 7→8 HIGH rows, vn-canu→vn-deep-dive-report backup-sprawl with U3 retired, live-hook figures re-derived to 45 event×script pairs / ~30 scripts, SUPERSET 7→8, ladder 781→771, keep-on 40→41, C6 withdrawn as a false finding). Full verdict: `validation-260831-0339-instruction-audit.md`.
