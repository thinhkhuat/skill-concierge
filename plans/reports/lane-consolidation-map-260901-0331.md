# Item 5 — lane-consolidation decision map (audit roadmap, evidence-ranked)

Date: 2026-09-01 03:35 · Method: caller census (word-boundary grep of every candidate name across `~/.claude/skills`, `skills-dev`, `~/.claude/commands`, `~/.claude/plugins/cache`, `settings.json`) + the verified agents-styles leaf (81 citations) for the overlap evidence. Every verdict names its caller evidence. Read-only; no file changed by this map.

**Headline the evidence forces:** the audit's "5 agents for one capability" reads as overlap in *prose*, but the *caller census* shows every candidate is spawned by a live system. The honest map is one MERGE, one shared-text extraction, and KEEP for the rest — plus a coding-ladder content fix that belongs to item 6, not to consolidation.

## Verdicts

| Candidate | Verdict | Caller evidence | Rationale |
|---|---|---|---|
| `explore` | **KEEP** | 35 skill refs + 168 plugin refs; also the harness's built-in Task(Explore) type | Heavily loaded; touching it breaks the most callers on the shelf. |
| `researcher` | **KEEP** | Spawned by `coding-agent-orchestration`, `claude-code`, `firecrawl-website-design-clone` | Distinct niche from explore: web research + synthesis vs local scan. Prose overlap ≠ lane overlap. |
| `librarian-cataloger` | **KEEP** | Called by `which-skills/references/library-and-scoring.md` | 29 lines, dedicated caller, single purpose (read-only skill cataloging). Not an orphan. |
| `goal-scout` + `goal-judge` | ~~MERGE~~ **NO CHANGE — withdrawn 2026-09-01 on full-source evidence** (owner confirmed) | Both spawned by `goal-prep/references/goal-execution.md` (GoalBuddy) | Full read of both files + the dispatch contract overturned the leaf's quote-depth "80% shared" reading: the agents share only the ~6-line read-only fence; Scout (low effort, evidence receipt, 12-command budget) and Judge (high effort, decision/worker_package schema, subgoal/parallel gates) are distinct roles. The PM dispatch mandates exact `subagent_type` names with SKILL.md fallback states, and codex `.toml` twins exist (agents/README table) — a merge is a portable-system redesign, not a file dedup. Owner picked no-change. |
| `goal-worker` | **KEEP** | Same GoalBuddy system | Different job (bounded writer, `allowed_files`); merging it with read-only siblings would blur a trust boundary. |
| `agent-validator` | **KEEP** | Spawned by `opus-validate` skill; 95 plugin refs (upstream agent-skills kit) | Active in the validation lane you used twice this arc. |
| `tk-validator` | **KEEP** | 3 refs in `skills-dev/tk-research` family | Distinct mission (tk-research briefings). |
| `vn-validator` | **KEEP** | 4 installed + 70 dev refs (7 vn skills spawn it) | The busiest validator. |
| validator trio shared text | **EXTRACT** (your call) | The anti-hallucination gate prose is duplicated verbatim ×3 (leaf M-20) | Not agent deletion: move the shared gate block to one disclosed reference (e.g. `~/.claude/skills/_shared-vn/validation-gate.md` pattern) referenced by all three; one-place edit for future gate changes. |
| `brainstormer` | **KEEP** | Referenced by `ak-agentkit/references/subagent-timing.md`, `ak-xia/SKILL.md` | Used by ak workflows despite low prose differentiation from `ak-brainstorm` skill. |
| `planner` | **KEEP** | 33 skill refs, 7 dev refs, 136 plugin refs | Heavily loaded. |
| `code-reviewer` | **KEEP** | 30 skill refs, 393 plugin refs | Most-referenced agent on the machine. |
| `code-simplifier` | **KEEP** | Called by `ak-cook/references/{subagent-patterns,workflow-steps}.md` | Part of the ak-cook pipeline — retiring it breaks a live workflow. |
| `coding-level-0` … `coding-level-5` (6 styles) | **KEEP files, fix content in item 6** | Only 2 external refs; selection is manual (`/output-style`) | Output styles are one-file-per-style by mechanism — no merge target exists. The real defect (153 decorative absolutes, 771 lines) is item-6 rewrite work, not lane consolidation. |
| ocx-* ×5 | **OUT OF SCOPE** | Generated by opencodex from its roster (`agents-inject.ts`) | Editing them is futile (regenerated); roster-level config only. |

## What this map does NOT recommend (and why)

- No RETIRE verdicts anywhere: the census found zero orphan agents — every candidate has at least one live caller. Deleting any would break a referenced workflow. RETIRE stays on the table only for a future candidate whose caller census comes back empty.
- No validator merge into one agent: three missions are genuinely distinct (general / tk / vn); only their shared gate *text* should be single-sourced.

## Decisions needed from you (pick per line)

1. **goal-scout + goal-judge**: MERGE as recommended (one two-phase agent), keep separate but extract shared fence, or leave as-is.
2. **Validator gate extraction**: extract shared text to one reference now, or defer.
3. Confirm KEEP-all elsewhere — execution after your picks is a small, single-session change.
